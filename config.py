from numpy import random

# decide nudity level:

n = 0  # 0 is nude, 1 is barely/occasionally, 2 is completely non-nude
# n = random.randint(0, 3)  # un-hash for completely random choice

# specify number of days for cum schedule
days = 30  # change to any integer

#specify pause in cum schedule list (in seconds)
sleep = 0
